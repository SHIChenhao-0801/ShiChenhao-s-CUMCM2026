# A题题意对齐


本文件为正式S1审题契约。题面及7份资产哈希已逐一核对；尚未运行生产求解，不包含真实模拟结果。

## 四问之间的关系

Q1是短时固定圆柱基线；Q2复用守恒结构但从初值开始全程使用附录3；Q3在Q2解上寻找全域达标事件；Q4同时更换附录4物性并加入半径收缩。Q4对Q3的差异不能单独归因于收缩。

## 逐问输入输出

| 问题 | 输入和模型增量 | 可量化输出 | 关键验收 |
|---|---|---|---|

| 问题一：预热平衡阶段 | 题给圆柱尺寸与均匀初值；附件1时间、烘房温度与水分；附录2物性 | 在0—1800s求固定圆柱T(r,t)、C(r,t)，使用附录2，输出7个时刻×5个半径的两张表及逐秒/0.1cm完整网格。 | 数值收敛、通量/质量守恒、模板采样；无内部实测真值 |

| 问题二：全过程热湿耦合 | 题给圆柱尺寸与均匀初值；附件1时间、烘房温度与水分；附录3物性 | 从同一初值出发，全过程统一使用附录3，求T/C双向物性耦合，正文显示前3h，文件保留逐秒/0.1cm完整解。 | 数值收敛、通量/质量守恒、模板采样；无内部实测真值 |

| 问题三：全域达标时间 | 题给圆柱尺寸与均匀初值；附件1时间、烘房温度与水分；附录3物性 | 沿用问题二全过程模型，在整个空间域max(C)<0.15条件下确定结束时间；按6h及60s导出含水率。 | 数值收敛、通量/质量守恒、模板采样；无内部实测真值 |

| 问题四：收缩下的达标时间 | 题给圆柱尺寸与均匀初值；附件1时间、烘房温度与水分；附录4物性；附件2时间与药材半径 | 使用附录4整组物性及附件2半径轨迹，建立与材料收缩守恒相容的模型，求全域达标时刻及移动域内C。 | 数值收敛、通量/质量守恒、模板采样；无内部实测真值 |

## 问题一：预热平衡阶段

- 基线：固定半径常系数圆柱Robin-Bessel解析解（热方程可直接验证，水扩散D冻结作求解器基准）

- 主模型：固定骨架一维径向热传导及D(C)非线性扩散；守恒有限体积+隐式时间积分

- 自变量：t:时间/s；r:物理径向距离/m。因变量：T(r,t):温度/K；C(r,t):干基含水率/kg水每kg干物质。

- 输出契约：`result1.xlsx`，详见JSON；内部全精度、展示四位小数。

- 验证：每条公式先说明目的、自变量、因变量、单位和物理含义；经验系数只作题给，不能伪造第一性原理推导；对源PDF第3—4页人工可视核对指数中的除号；温度进入Arrhenius项前必须换算为K；检查初始场、中心零通量、表面Robin通量、C正性及温度/含水率范围；不以数值裁剪掩盖失败；常系数圆柱Bessel级数对照验证离散求解器，报告截断误差与数值误差的区别；分别加密空间网格和时间步；满足渐近收敛条件才报告Richardson外推或观测阶；检查累计水分流出量与干基总水量减少的离散闭合；核对单位与相对残差；保存输入/代码/参数/输出SHA256、实际运行退出码与有限数值；VS GUI复现、用户人工审查分别记录；没有药材内部T/C实测真值；不得将数值一致性写为实测RMSE、预测准确率或校准成功

## 问题二：全过程热湿耦合

- 基线：固定半径、同一初值的常物性圆柱热/水基线，作为附录3变物性耦合的对照

- 主模型：固定骨架一维径向非线性热湿耦合PDE；rho*cp与k依赖C、D依赖C及绝对温度

- 自变量：t:时间/s；r:物理径向距离/m。因变量：T(r,t):温度/K；C(r,t):干基含水率/kg水每kg干物质。

- 输出契约：`result2.xlsx`，详见JSON；内部全精度、展示四位小数。

- 验证：每条公式先说明目的、自变量、因变量、单位和物理含义；经验系数只作题给，不能伪造第一性原理推导；对源PDF第3—4页人工可视核对指数中的除号；温度进入Arrhenius项前必须换算为K；检查初始场、中心零通量、表面Robin通量、C正性及温度/含水率范围；不以数值裁剪掩盖失败；常系数圆柱Bessel级数对照验证离散求解器，报告截断误差与数值误差的区别；分别加密空间网格和时间步；满足渐近收敛条件才报告Richardson外推或观测阶；检查累计水分流出量与干基总水量减少的离散闭合；核对单位与相对残差；保存输入/代码/参数/输出SHA256、实际运行退出码与有限数值；VS GUI复现、用户人工审查分别记录；没有药材内部T/C实测真值；不得将数值一致性写为实测RMSE、预测准确率或校准成功；50°C/0.05平台、末值、末1h均值延拓与h/beta情景对照

## 问题三：全域达标时间

- 基线：问题二已验证解的max(C)阈值事件；常D/Bessel时长仅作可解释尺度对照

- 主模型：继承Q2的守恒数值解并对全域最大干基含水率做阈值事件定位

- 自变量：t:时间/s；r:物理径向距离/m。因变量：T(r,t):温度/K；C(r,t):干基含水率/kg水每kg干物质；t*:全域达标时间/s（最后转h）。

- 输出契约：`result3.xlsx`，详见JSON；内部全精度、展示四位小数。

- 验证：每条公式先说明目的、自变量、因变量、单位和物理含义；经验系数只作题给，不能伪造第一性原理推导；对源PDF第3—4页人工可视核对指数中的除号；温度进入Arrhenius项前必须换算为K；检查初始场、中心零通量、表面Robin通量、C正性及温度/含水率范围；不以数值裁剪掩盖失败；常系数圆柱Bessel级数对照验证离散求解器，报告截断误差与数值误差的区别；分别加密空间网格和时间步；满足渐近收敛条件才报告Richardson外推或观测阶；检查累计水分流出量与干基总水量减少的离散闭合；核对单位与相对残差；保存输入/代码/参数/输出SHA256、实际运行退出码与有限数值；VS GUI复现、用户人工审查分别记录；没有药材内部T/C实测真值；不得将数值一致性写为实测RMSE、预测准确率或校准成功；50°C/0.05平台、末值、末1h均值延拓与h/beta情景对照；全域最大值而非均值判定；中心是否为最大值逐时验证，不能直接假定；分别报告事件插值误差、时间步误差、空间误差及模型情景差异

## 问题四：收缩下的达标时间

- 基线：附录4相同物性、固定R0、同边界的全过程模型，用于隔离半径收缩的净影响

- 主模型：同比径向材料收缩x=r/R(t)的一维热湿耦合PDE；rho_d随R^-2变化守恒，有限体积在固定材料网格求解

- 自变量：t:时间/s；r:物理径向距离/m；x=r/R(t):材料径向坐标/1。因变量：T(r,t):温度/K；C(r,t):干基含水率/kg水每kg干物质；t*:全域达标时间/s（最后转h）。

- 输出契约：`result4.xlsx`，详见JSON；内部全精度、展示四位小数。

- 验证：每条公式先说明目的、自变量、因变量、单位和物理含义；经验系数只作题给，不能伪造第一性原理推导；对源PDF第3—4页人工可视核对指数中的除号；温度进入Arrhenius项前必须换算为K；检查初始场、中心零通量、表面Robin通量、C正性及温度/含水率范围；不以数值裁剪掩盖失败；常系数圆柱Bessel级数对照验证离散求解器，报告截断误差与数值误差的区别；分别加密空间网格和时间步；满足渐近收敛条件才报告Richardson外推或观测阶；检查累计水分流出量与干基总水量减少的离散闭合；核对单位与相对残差；保存输入/代码/参数/输出SHA256、实际运行退出码与有限数值；VS GUI复现、用户人工审查分别记录；没有药材内部T/C实测真值；不得将数值一致性写为实测RMSE、预测准确率或校准成功；50°C/0.05平台、末值、末1h均值延拓与h/beta情景对照；全域最大值而非均值判定；中心是否为最大值逐时验证，不能直接假定；分别报告事件插值误差、时间步误差、空间误差及模型情景差异；附录4固定半径与收缩半径同物性对照；Q3/Q4跨物性对比单列；半径数据0—72h覆盖检查、物理坐标输出映射和域外空值核验

## 数据能支撑到哪里

附件1的241条记录仅覆盖0—4h，每60s一个环境点；附件2的145条记录覆盖0—72h，每1800s一个半径点。两者都是驱动条件，不是药材内部温度或含水率观测。由数据直接支持的是边界和尺寸；从一维几何、等效平衡映射、材料速度场和显热闭合起就已经加入假设，绝不是等到4h后才开始假设。插值后的逐秒驱动是模型输入重采样，不能增加独立实测样本数。4h后的温湿平台、72h后的任何半径外推必须单独标记。

## 模型假设起点与影响

- **A01 径向一维及端面处理**：采用轴对称、轴向近似均匀的一维径向场，固定长度L=0.25m，Q1—Q3半径R0=0.02m。 起点：由真实三维药材抽象为圆柱的一维传递域时。依据：题目按到中心的距离输出；长径比L/(2R0)=6.25。限制：题目有限圆柱事实不等于端面绝热；端面积/侧面积=R0/L=0.08，不足以独自证明影响可忽略。检验：至少量级分析；有预算时采用有限圆柱二维轴对称或可核验常系数端面修正情景。

- **A02 有效显热**：用a(C)=rho(C)*cp(C)作为有效体积热容量，a(C)D_tT=div(k(C)grad(T))；基准不显式增加相变潜热或湿分携带焓。 起点：从总能量守恒约化到有效显热方程时。依据：题给rho、cp、k及对流换热参数，未给完整相变/焓闭合。限制：rho(C)在此作为题给有效热容量的组成部分，不能同时无条件声明其为严格守恒的湿物料总密度。检验：表面潜热替代情景需明确j_w、Lv及能量方向，禁止内外双重扣潜热。

- **A03 干物质骨架及Fick闭合**：C为kg水/kg干物质。Q1—Q3固定均匀干骨架；Q4均匀同比径向材料收缩，rho_d(t)=rho_d0*(R0/R(t))^2。水通量j_w=-rho_d*D*grad(C)。 起点：把干基质量比C转为守恒水质量，并为通量选择本构时。依据：干物质质量守恒与给定半径轨迹可相容闭合。限制：rho_d0仅决定总水质量单位尺度，不影响空间均匀rho_d下的C解；不能把rho(C)/(1+C)再强塞入同一均匀骨架。检验：干物质守恒、累计水分质量守恒，必要时对替代总密度闭合单独立模。

- **A04 环境到材料的等效平衡映射**：基准令Ceq(t)=附件1给出的烘房水分值，使用-D*C_r=beta*(Cs-Ceq)。 起点：把空气观测变为材料表面Robin边界时，早于4h外推。依据：题目提供kg/kg环境水分与m/s有效传质系数，未给吸附等温线。限制：kg水/kg干空气与kg水/kg干药材不是同一基准；这里是简化约定，并非由单位相同推得物理等价。检验：对Ceq映射比例/偏移或不同平衡值做敏感性；没有品种吸附数据不能唯一恢复真实映射。

- **A05 环境插值和4h后延拓**：0—14400s按附件1分段线性插值，超过4h基准取50°C与Ceq=0.05。 起点：相邻60s观测之间采用插值时已有局部假设；超过14400s明确进入观测外延拓。依据：附件末时段接近恒温、恒湿平台，题面称恒温干燥。限制：50/0.05只是候选工况，不是其后68h的实测值，也不能声称给出了统计置信区间。检验：末值保持、末1h均值保持、平台温湿扰动；时长对工况变化另列。

- **A06 收缩材料运动**：Q4以附件2的R(t)确定边界，取u(r,t)=r*R'(t)/R(t)，长度固定。x=r/R(t)为材料坐标，网格速度等于骨架速度。 起点：由仅有半径观测推断内部材料速度场时。依据：与表面速度及空间均匀干物质骨架守恒相容的最简闭合。限制：附件没有内部位移或长度轨迹，不能证明真实内部收缩均匀；仅坐标变换本身不构成物理假设证据。检验：R插值保持非增、正值；同物性固定/收缩对照；不得重复加入ALE平流或C体积稀释项。

- **A07 传热传质系数延用**：Q2—Q4沿用h=25 W/(m²K)、beta=8e-7m/s。 起点：附录3/4未重给边界系数，建立对应边界条件时。依据：同一药材烘房，题面仅明确更换给出的经验物性。限制：温湿风速变化可能改变系数，当前无风速或系数测量。检验：h、beta独立±10%/±20%参数情景，报告设置而非实测误差。

## 待解释口径

- **U01**：烘房kg/kg的参考质量基准与材料平衡含水率映射未明。处理：按A04显式约定并敏感性，不能隐去。状态：不阻塞条件性机理求解；阻塞无条件的实测预测准确性声明。

- **U02**：附件1仅4h而全过程约2—3天。处理：按A05平台延拓并对照末值/末段均值/扰动。状态：无假设不能唯一预测4h以后。

- **U03**：result2完整结果的终止时刻未明确，正文只列3h。处理：保留从0至Q3达标时刻的逐秒全程T/C；另导出前3h，依据20M支撑上限实测压缩体积后形成明确提交方案。状态：不影响求解；最终文件范围解释与体积仍待导出验证。

- **U04**：Q4半径不落0.1cm或0.5cm格点时表面及域外单元如何表达。处理：固定物理距离列+独立表面列，域外留空且不得填0；配套时间到R(t)映射/元数据，最终保证模板格式清晰。状态：最终模板结构需实际导出检查。

- **U05**：rho(C)与干基/固定或给定收缩体积的严格总质量守恒不自动相容。处理：有效热容量和干骨架质量守恒分清；另一密度解释必须新建并标明替代模型。状态：阻塞把相互不相容闭合全部称为严格守恒。

- **U06**：Q4若达标超过72h则R数据不足。处理：先在实测范围内求解；超出时标记数据域外并比较末半径保持与正值非增外推，不能静默延长。状态：72h后时长依赖额外半径外推。

- **U07**：严格低于0.15和四位小数显示0.1500可能冲突。处理：事件求根报告连续阈值交叉时间、严格达标的首个输出时刻及未舍入max(C)；等于阈值点不称严格低于。状态：不阻塞模型；需保留未舍入判定证据。

## 公式和放缩交接

题面附录显式给出9条经验式：1+4+4。题给常数和建立模型需要的守恒、通量、边界、坐标变换、离散及检验公式另外编号，完整清单由物理推导交接文件给出；不声称经验式的拟合系数可由守恒定律推导。

放缩首先区分单位换算、无量纲化、空间域映射和不等式界估计。SI单位換算为必要操作；Q4采用x=r/R(t)固定移动域；T/C数值缩放用于误差控制，恢复后按物理单位导出。D(C,T)上下界仅用于量级/可比较模型分析，不能把非线性PDE的逐点解排序当作当然结论。无需对环境列做机器学习式z-score来替代物理模型。
