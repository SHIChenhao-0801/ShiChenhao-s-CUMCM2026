# 用户人工代码审查单

状态：**待用户逐项查看与签核**。AI检查、CLI运行、VS断点观察和MATLAB输出均不代替此表。

## 建议先审查的关键位置

| 顺序 | 打开位置 | 应当核实的行为 | 人工结论 |
|---|---|---|---|
| 1 | `q1Model.solve`、`q2Model.solve`、`q4Model.solve` | 附录、N、rtol/atol、步长、Kirchhoff开关；Q4 shrink=True | 待审 |
| 2 | `dryingCore.loadInputs` / `environment` | 秒、开尔文、米和干基单位；4小时后50°C/0.05是假设 | 待审 |
| 3 | `RadialModel.properties` | 指数是除以C与T_K；湿基C/(1+C)；三组物性系数 | 待审 |
| 4 | `waterInternalFlux` | Kirchhoff势函数、相消时中点展开、温度因子位于差分外 | 待审 |
| 5 | `RadialModel.rhs` | 中心零面面积、表面Robin负号、R²几何因子、干物质另守恒 | 待审 |
| 6 | `analyticJacobian.jacobian` | 水热导数、容量分母导数、稀疏行列索引和最终失水状态 | 待审 |
| 7 | `solveCaseImpl` | 4h切段、BDF、事件方向-1、全域最大C、事件后真实续算 | 待审 |
| 8 | `q3Model.completion` | 0.0001h向上取整后重新查询未舍入C，验证严格小于0.15 | 待审 |
| 9 | `diskDense` | 原始float64多项式、BDF选段方向、关闭映射/清理私有缓存 | 待审 |
| 10 | `Run.fields` / `exportOutputs.fieldBlock` | 材料坐标与物理半径不同；Q4域外NaN→空白、表面独立列 | 待审 |
| 11 | `exportQuestion` / `validateExports` | 每秒/每60秒完整日程、逐格回读、正文表采样单位；`completion_source`必须指向实际 `q3Model.py`及其SHA256 | 待审 |
| 12 | `runDelivery.runQuestion` / `runLogged.ps1` | 一轨迹一次求解、输入哈希、版本目录、真实退出/异常留痕 | 待审 |
| 13 | `matlab/runCrossCheck.m` | 独立expint符号、数组方向、ode15s事件、N40与正式结果区别 | 待审 |
| 14 | Python/MATLAB取样对照 | 按实际时间和材料坐标对齐；连续事件与严格上取时刻不能按行直接相比 | 待审 |
| 15 | `runResult.json`、`driver_<runId>/processResult.json`及GUI观察 | 区分内部PASS、真实退出、断点观察、完整GUI运行和本人人工接受；保留v1失败和v2修复证据 | 待审 |

## VS调试时建议查看的变量

- 初次进入RHS：`t=0`，温度数组为301.15 K，水分数组为2.55，半径0.02 m；Q1物性ρ=820、cp=2600、k=0.36，D约4.93765509×10⁻⁹ m²/s。
- `model.settings`：Q1/Q23正式N3200，Q4正式N6400；`faceScheme='kirchhoff'`，`jacobianMode='analytic'`，`denseStorage='disk'`。
- 第一个控制体和最后一个控制体的`w`、`heatG`、`waterG`：中心零通量，表面失水对应waterG末项负、累计失水导数正。
- `solutionRun.eventS`、`summary['completion']`：事件根与严格报告时刻不同；Q3/Q4共用判定函数。
- 输出时`inside`、`surfaceC`：固定2cm可能在Q4材料之外，不能把它当移动表面。

## 接受记录

本轮首版曾因导出器来源路径保留旧文件名而在Q3复核失败，实际退出1；N40等价性检查与初次AI审查未覆盖该失败路径。修复后Q3/Q4定向导出验证通过，范围见[路径修复证据](../runtime/exportPathFix_v2/verification.json)，仍须核对最终细网格版本与GUI完成记录。此事实不由清单预填为人工“接受”。

请在自己完成阅读后填入：审查人、北京时间、审查源码SHA256或Git提交、接受/需修改项目、仍保留的模型假设、运行环境与输出版本。任何改动模型/核心计算代码/输入都应新建运行编号重算、复核；原运行日志保留。

审查人：________　时间：________　源码版本：________

结论与修改意见：________
