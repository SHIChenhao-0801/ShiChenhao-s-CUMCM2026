"""Apply the agent's final prose revision after deterministic assembly.

The supplied front matter and complete code listings are preserved. Revisions
unify argument order, interpretations, evidence scope and appendix references.
"""
from pathlib import Path
import json
import hashlib

ROOT=Path(__file__).resolve().parents[1]
source=ROOT/'paper_output/drafts/assembled_draft.md'
text=source.read_text(encoding='utf-8')
changes=[
('题给ρ与cp的乘积记为有效体积显热容量B。用这一系数描述局部升温所需显热，是本题基线的热学闭合；水分质量守恒则单独使用ρd。不能仅因ρ的单位为kg/m³，就再把ρ/(1+C)无条件代入同一个均匀骨架模型。这样区分的原因是：题给物性、固定长度、给定收缩轨迹和严格干质量守恒并不自动相容，详细计算见附录A.2。本文据此保留可求解、可解释的有效热湿模型，避免在同一推导中混用两种密度。',
 '热学和质量方程需要分别说明密度的用途。题给ρ与cp的乘积记为B，用来描述单位体积温升的有效显热需求；ρd则用于计算随材料守恒的干物质和水质量。若把题给ρ同时解释为真实湿密度，再强行令ρd=ρ/(1+C)，会对固定长度和给定半径施加额外约束。附录A.2表明，该约束与所给后期收缩数据不能同时满足。因此本文采用有效容量与独立干骨架的闭合，使两套方程的定义一致；这一处理也限定了结果的物理解释，不能视为对真实组分密度的验证。'),
('离散温度和含水率交错组成状态向量，并增加一个累计失水变量。程序利用相邻节点耦合形成的稀疏Jacobian，通过隐式BDF方法推进；BDF适合本题细网格扩散带来的刚性[6]。其具体残量、Jacobian块和边界项在附录B给出。输出间隔只决定在哪些时刻查询数值轨迹，不强迫每个积分步等于1 s。',
 '细网格缩短了局部扩散时间尺度，显式推进会受到较严的步长限制。本文将温度和含水率交错排列，利用相邻节点耦合构造稀疏Jacobian，再由隐式BDF联立求解[6]。状态末项只累计失水，不反馈改变温湿方程，可用来核对全域水量收支。附录B展开残量、矩阵及边界导数。求解器在误差控制下选择积分步长，正文和工作簿则查询其连续输出，两者分开处理，保证1 s输出并非由稀疏表格重新拼接。'),
('此时温度影响水分扩散，含水率又改变热容量和导热系数，构成双向耦合。即便两问采用相同环境输入，内部温度和水分也不必相同。联立求解能够将每一步更新的T、C一致地反馈到物性中；分别先算完整温度曲线再代入水方程，会遗漏水分变化对热场的作用。',
 '附录3使两个状态之间出现双向反馈：T进入扩散系数，C同时进入热容量和导热系数。在一次时间推进中，新的温度会影响水通量，新的含水率又改变温升速度和热通量。因此即使外部环境完全相同，问题二也不能沿用问题一的温度轨迹。联立算法在同一时刻更新T、C与全部物性，使耦合关系进入隐式残量；先独立计算全程热场再求水分，会改变这里采用的模型。'),
('表5末行中心显示0.1500，是未舍入值保留四位后的结果；其严格性已在原精度上判断。不能为使视觉上“小于0.15”而把表格改写为0.1499，这会改变计算结果。',
 '表5末行需结合报告约定阅读。未舍入的最大浓度严格小于0.15，而保留四位后的中心单元格仍可显示0.1500；显示精度不足以区分与阈值的微小差距。本文保留这个真实舍入结果，并用事件根、报告时刻和原精度查询完成判定。把单元格人为改成0.1499虽然看似符合不等式，却会破坏数值输出与约束核验之间的对应关系。'),
('收缩使式(14)中的扩散时间尺度随R²降低，同时改变表面传质项与内部扩散的相对作用。以R²构造的时标换算与同物性定半径对照相差约1.37%，为迁移距离解释提供了尺度上的支持。但该积分上限取自仿真事件，不能称为完全独立的解析预测。问题四仍有约92.17%的时间位于环境观测窗之后，结果依赖式(10)的长期工况假设。',
 '式(14)直接显示收缩为何会加快内部传递：在相同局部物性下，径向扩散项随R⁻²增强；式(16)又表明表面交换与内部传递的相对比例也在变化。R²时标换算与同物性定半径对照相差约1.37%，支持迁移路径缩短的尺度解释。由于换算积分使用了仿真事件作为上限，它是机理核对而非额外的独立解析时长。几何数据覆盖达标时刻，但环境数据没有：约92.17%的时长仍处于式(10)的平台延拓中，两类观测范围必须分清。'),
('在数值上，环形有限体积使内部面通量成对相消，Kirchhoff势处理浓度方向的强非线性，解析稀疏Jacobian降低细网格隐式计算的求解负担。空间与时间设置对照、解析基准及独立实现形成不同层次的核验。原精度轨迹、四位显示和严格达标判断分别记录，有助于避免因表格舍入造成的错误结论。',
 '数值方法围绕本题的困难作了对应处理。圆柱控制体承担中心和边界的守恒离散，Kirchhoff势处理低含水率区的非线性面传递，稀疏Jacobian和磁盘连续输出分别降低隐式求解与保存高网格轨迹的负担。检验则覆盖解析极限、空间和时间设置、独立实现等环节。最终将原精度轨迹、显示舍入与严格判据分别记录，使结果表能够追溯到实际计算，而不依赖后期人工修饰数字。'),
('主要限制来自物理闭合和观测范围。附件没有内部温湿度或称重曲线，无法直接评价真实预测误差；环境记录仅覆盖4 h，长时程主要依赖恒温恒湿平台假设；空气指标到药材平衡含水率的映射未由材料等温线识别。给定半径只描述边界变化，均匀内部收缩和固定长度仍是假设。改变这些条件可能影响时长，不能用较小数值误差掩盖它们。',
 '计算设置稳定之后，主要不确定性仍来自模型输入的解释。没有内部温湿度或称重曲线，就无法用观测残差评价预测准确性；4 h之后没有环境记录，就必须依赖长期工况假设；没有药材等温线，就无法从空气指标唯一确定材料平衡状态。此外，半径观测只约束表面，不能单独证明内部同比收缩或轴向长度恒定。这些条件与数值网格差属于不同来源，需要在未来实验中分别识别。')
]
records=[]
for old,new in changes:
    assert text.count(old)==1,old[:40]
    text=text.replace(old,new)
    records.append({'before':old,'after':new})
out=ROOT/'paper_output/final_paper_source.md'
out.write_text(text,encoding='utf-8')
(ROOT/'paper_output/qa/manuscript_20260912/global_revision_record.json').write_text(json.dumps({
    'assembledSha256':hashlib.sha256(source.read_bytes()).hexdigest(),
    'finalSha256':hashlib.sha256(out.read_bytes()).hexdigest(),
    'purpose':'Unify causal explanations, model definitions, evidence scope and result interpretation while preserving user text and complete code.',
    'paragraphRevisions':records},ensure_ascii=False,indent=2),encoding='utf-8')
