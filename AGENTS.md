# 2026 CUMCM 正式比赛工作区

本文件在父目录 `AGENTS.md` 的流程、人工审查及隐私边界要求上增加本届比赛的具体约定。

## 工作路径与时间

- 用户指定：**2026-09-10 18:00（北京时间）起，本次比赛所有新增文件和工作均在 `D:\Document\数学建模\2026CUMCM` 及其子目录进行。** 终端工具的 `workdir` 必须设到本目录或其子目录；不能仅在文字中说已切换。
- 父目录的资料、软件和 Skill 固定版本可作为只读输入。需要修改或正式采用的历史代码/数据先复制进本目录，记录来源和哈希；模型、论文、图表、日志、下载、AI 记录、聊天摘要、进度简报和交接均写在这里。
- 父目录旧 `paper_output` 是练习/历史状态，不得继承其 S0–S7/PASS；新比赛的题目、数据与进度从实际证据建立。
- 本次正式状态仅查看本目录的 `paper_output/context/workflow_memory.json` 和相应门禁/运行记录。没有题目时明确记录尚未开始，不虚构完成阶段。
- 这是父项目内的子工作区，不是全局记忆；不导入其他项目内容。原 Codex 会话和侧栏由应用管理。

## 用户指定的资料优先级

- 用户于 2026-09-10 明确指定：**最新的 9 月 10 日赛前说明会是首要执行依据，所有与之冲突的既有资料、记忆、模板和安排均按说明会更新。** 原件位于 `reference_materials/contest-admin/20260910“高教社杯”全国大学生数学竞赛赛前说明会.pdf`。
- 保留讲义中的“必须”“建议”和历史例子的语气，不把经验建议改称全国硬规则。讲义自身冲突需记录页码、不同说法及待校方澄清状态；不能自行制造确定答案。
- 当前执行：9 月 10 日 18:00 开赛；9 月 13 日 19:00 前完成 MD5 为讲义建议目标，20:00 为硬截止；9 月 13 日 20:30 至 9 月 14 日 14:00 上传同一份定稿文件。
- 论文使用 PDF；正文不超过 30 页，含 AI 声明及参考文献，摘要/附录另计。按讲义第 76 页落实摘要至参考文献至少 15 页的校内要求，20–30 页为建议范围；第 38 页“35 页”等内部矛盾保留记录，不放宽 30 页上限。AI 声明放在参考文献之前，支撑包含 `AI工具使用详情.pdf`。
- 旗山讲义两页开门时间分别为 9 月 10 日 17:00/17:30、9 月 11 日 07:00/06:00，待校内明确；9 月 11 日现场说明会只知上午或下午。不能把旧笔记推定时间当作已确认通知。

## 实施与协作

- 延续规则审题→数据审计→变量/假设→可解释基线与模型比较→实现→实际验证→结果冻结→论文→独立终审。任何数据/模型更改触发相关结果重算、复核与文稿更新。
- 用户主导关键模型与结论；论文同学同步写结构/方法并负责图表编排、一致性。交接必须带输入、代码、结果和检验版本；未经运行数值不能填写成真实结论。
- 比赛 Python/C++ 必须通过 Computer Use 在 **Visual Studio** 可视化复现，核心代码由用户人工审查；CLI 成功和 AI 阅读均不能替代这两项。MATLAB/R 采用结果也需人工核实。
- 既有 MATLAB/R/VS 环境小样只证明相应基础能力，不证明正式题代码有效。默认 CPU 与有限实验预算。
- `.agents/skills` 只复用父目录当前启用的一套 Skill，联接仅用于读取共享工具；不能通过联接改写父目录正式结果或共享 Skill。共享脚本必须在本目录为 cwd 时运行。
- 共享 Python 脚本使用 `-B` 或将 `PYTHONPYCACHEPREFIX` 设到本目录 `tmp/cache/python`，防止运行时把字节码写回共享 Skill；绘图缓存可将 `MPLCONFIGDIR` 设到本目录 `tmp/cache/matplotlib`。这些仅用于任务进程，不修改系统级环境设置。
- 可重建缓存放 `tmp/cache/`；原始题面、输入、运行证据、冻结结果、稿件、配置、会话记忆不是缓存。

## GitHub 版本备份

- 用户于 2026-09-10 要求：本次产生的代码和文件均在其 GitHub 仓库保留版本。正式仓库根目录就是本目录；远端 `origin` 为 `https://github.com/SHIChenhao-0801/ShiChenhao-s-CUMCM2026.git`，分支 `main`。设置时已核实为私有仓库。
- 后续每完成一个可复核工作单元，检查文件差异、凭据及大文件后 commit 并 push；可使用 `tools/git-sync.ps1 -Message '说明'`。本条是持续同步授权，无需每次重复询问。由主代理统一提交，子代理不并发操作 Git 索引或推送。
- 按 `.gitignore` 排除凭据、可重建缓存、机器环境、父目录 Skill 联接和已列明的 B 题外部程序/演示。源码、数据、来源、论文、图表、结果、日志证据、AI 记录、记忆及支撑文件默认纳入；任何新增大文件或异常不能静默遗漏。
- 同步不改变模型/论文审核状态。不得强推、丢弃其他人的更改、公开私有仓库或把登录令牌写入文件。网络/权限/冲突导致推送未完成时，保留本地工作并明确说明；只有核实远端提交一致才声称 GitHub 已备份。
- 用户随后要求每 6 小时自动同步一次，以协调世界时 UTC 10:00 为锚点。已创建并启用当前线程的 Codex heartbeat `2026-6-git`：每天 UTC 04:00/10:00/16:00/22:00，即北京时间 00:00/06:00/12:00/18:00 执行检查、commit/push 及远端核验。无变化不创建空提交；没有变化且无需处理时保持安静，有新提交上传、失败或需人工处理时才通知。
- 定时任务执行前必须确认没有并发 Git 操作、锁或未完成的合并/变基等；不删除锁、不终止计算、不抢占正在写入的成果。当前无自动停止日期，后续按用户要求调整。详细操作、排除清单和协作方法见 `notes/workspace-maintenance/github-sync.md`；保存文件本身仍不等于已推送。
