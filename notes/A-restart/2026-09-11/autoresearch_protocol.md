# A题独立重启：有预算重复实验协议

- 建立日期：2026-09-11；历史截止：2026-09-10 10:00 UTC（北京时间18:00）。本轮新指令和原始赛题/附件另行允许。旧求解源码、输出和模型路线均不作为输入。
- 技能：项目 `autoresearch` 适配器1.0.0，固定上游228791fb499afffb54b46200aca536f79142f117；本轮使用其“假设—固定评估—实验—保留/舍弃”方法，不训练语言模型。
- 最大预算：准备、运行、评估合计30分钟；CPU，单进程；测试齐备即结束，不为了耗尽预算继续试验。
- 当前阶段：原题审读、原始数据审计、公式建立及数值方法验证。不是正式四题全量生产和论文阶段。
- 原始输入：`problem_files/CUMCM2026Problems/A题/`。新审计与推导在本目录；R代码在`paper_output/code/a_restart/`；结果在`paper_output/results/a_restart/`。
- 物理闭合缺口先登记，不通过反复调参数拟合无实测的“真实结果”。

## 固定评估器

圆柱无量纲扩散方程 u_t=(x*u_x)_x/x，x∈[0,1]，中心对称，外边界-u_x=Bi*u。选择lambda=1、Bi=lambda*J1(lambda)/J0(lambda)，精确解exp(-lambda²*t)*J0(lambda*x)。以各环形控制体精确平均值初始化和比较。

唯一主指标：t=0.2时，控制体平均数值解相对于精确平均的最大绝对误差。所有候选保持相同初值、边界和终时；不把质量残差或速度换作主指标。

- 基线：N20圆柱有限体积+Crank–Nicolson，dt=0.002。
- 候选：仅网格和时间步改动；N40/N80/N160分别细化，独立重复最佳配置一次。
- 负对照：将圆柱几何错误换成平板；该候选即使运行正常也按物理约束不满足舍弃。
- 时间敏感性：同一N80比较两个dt。
- 留出：选择完毕后才对lambda=1.5、终时0.37的精确解核验；不依留出结果反复调参。
- 附加局部检查：非线性扩散必须保留D梯度项；固定域中心极限；均匀收缩时干基含水不应出现体积稀释项；Robin通量正负；物性单位/量级。
- 失败/非有限值/负状态异常/守恒失配不能替代有效最佳结果；保存所有实验记录。
- 记录命令、环境、代码SHA256、开始/结束、退出码、耗时、参数、误差、守恒残差、种子（不含随机环节，记不适用）。

## 结论权限

本实验验证圆柱离散方法、收敛趋势、重复性与局部公式，不能验证题目非线性四题的完整生产精度，也不能证明药材实际预测准确率。R按用户已授权Rscript路径实际运行；本轮无正式Python/C++求解器、无新的VS GUI复现、无用户人工代码审查。
