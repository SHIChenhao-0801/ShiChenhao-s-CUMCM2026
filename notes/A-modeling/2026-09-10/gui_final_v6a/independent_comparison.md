# final_v6a 与 gui_final_v6a 独立数值一致性审计

**结论：PASS_NUMERICAL_ARTIFACT_EQUIVALENCE，60项检查全部通过，没有数值差异。** 审计直接读取正式CLI生产 `final_v6a` 和Visual Studio独立复现 `gui_final_v6a` 的实际文件；没有调用模型、导出或原比较函数，没有重跑PDE，没有修改模型/结果目录或两份主MD。

实际审计运行于2026-09-10 **17:41:50.102897—17:41:53.162170 UTC**，耗时约3.06s。开始前已经存在GUI `review_result.json`，其状态为 `COMPUTED_PENDING_GUI_OBSERVATION`，包含完整Q1/Q23/Q4摘要；审计完成后再次检查该文件未变化。

**本审计认证已落盘数值产物一致，不认证Visual Studio进程已实际退出。** VS Output实际退出观察由主代理另留证据；GUI运行时间包含断点暂停，不用于与CLI作性能比较。团队/用户人工代码与模型审查仍待确认。产物一致也不证明真实药材预测精度或消除等效Ceq、有效热密度和潜热闭合缺口。

## 1. 源码、输入与配置

两次启动清单均为28项输入/源码，逐项路径、字节和SHA256完全一致；审计重新读取当前原件，这28项哈希亦全部匹配。运行环境及字体的两次记录相同。审计工具放在项目 `tools`，没有加入冻结的 `code/modeling` 清单。

逐轨迹核对实际 `settings`、核心/Jacobian/磁盘代码记录、数据输入、`completion`、`reproduction_samples`、`metric_samples`，均完全一致。全部数值诊断与计数也一致，唯一有意排除的是 `elapsed_s`，它受GUI停断点时间影响。三条GUI摘要与最终 `review_result.json` 对应摘要逐项相等，两侧均零求解器警告。

审计未把不同的运行ID、输出路径、生成时刻或压缩容器时间当成物理参数，也未忽略求解器实际配置。Q1/Q23实际N=3200，Q4实际N=6400。

## 2. 三条轨迹、事件与原始NPZ

| 轨迹 | 保存时刻数×材料坐标数 | T/C最大绝对差 | 其他保存数组 | NPZ容器本身 |
|---|---:|---:|---|---|
| Q1 | 32×21 | 0 | 时间、材料坐标、R、平均C、累计失水全部一致 | SHA256相同 |
| Q23 | 3452×21 | 0 | 同上 | SHA256相同 |
| Q4 | 3069×21 | 0 | 同上 | SHA256相同 |

比较使用 `np.load(..., allow_pickle=False)`；核对键、形状、dtype、全部数值及NaN位置，没有只抽首末行。上表是保存NPZ的采样数量，不冒称这些NPZ包含每个内部BDF接受步。逐秒完整出口另在下一节比较。

两次运行以下临界根、严格报告时刻和其浓度全相同：

| 问题 | 临界事件/s | 严格报告时长/h | 报告时刻/s | 原精度max C/(kg/kg) |
|---|---:|---:|---:|---:|
| Q3 | 206900.28702201758 | 57.4724 | 206900.64 | 0.14999989718225315 |
| Q4 | 183926.06923258994 | 51.0906 | 183926.16 | 0.14999995339076624 |

最慢材料坐标均x=0；事后复核末秒分别206902和183928。不能将临界根、报告时刻、最后复核秒合并为一个“结束时间”。

## 3. 四份完整原精度GZIP和XLSX

GZIP逐个完整解压，以流式SHA256及实际解压字节数核对内容。XLSX按ZIP条目完整解压，比对每个条目的字节数和SHA256；工作表XML、共享字符串/样式（若存在）、工作簿和关系条目均必须一致。仅允许 `docProps/core.xml` 中创建/修改日期不同，删除这两个日期元素后的XML仍须一致；不是笼统忽略所有属性。

| 问题 | 每侧原精度GZIP解压字节 | 完整解压内容一致 | 工作表XML条目数 | 所有工作表XML一致 | 两侧既有出口回读单元数 |
|---|---:|---|---:|---|---:|
| Q1 | 1445070 | 是 | 2 | 是 | 79244 |
| Q2 | 146776490 | 是 | 2 | 是 | 9103732 |
| Q3 | 1481115 | 是 | 1 | 是 | 75922 |
| Q4 | 965025 | 是 | 2 | 是 | 76700 |

合计150667700字节原精度文本、398111196字节工作表XML均两侧对应相同。每个工作簿的解压条目名集合一致，除上述创建/修改日期之外，其余解压条目全部字节一致。

四个GZIP外层SHA256不同，但完整解压内容相同；四个XLSX外层SHA256也不同，其中解压内容差异只在允许的创建/修改日期。故“文件哈希不同”本身不是数值不一致。另一方面，工作表XML、字符串表/样式及工作簿关系全相同，已足以支持完整数值和解释规则一致，无需再以openpyxl重复读取约933万个单元。

两侧各自的四份 `resultN.validation.json` 均为PASS、`fully_verified_with_live_Run=true`，回读单元数相同。本审计独立比较了全部XML字节；9335598个单元逐格对live Run/原精度投影的验证，是两次导出过程各自完成的检查，不冒称审计工具再次执行了相同回读。

Q2两表均从0到206902秒逐秒完整保存，非只有前三小时。Q4的真实表面、域外空白和半径工作表随对应XML/原精度文本一起核对，未把空白转为零。

## 4. 七份正文CSV及四张图的数据

七份正文CSV用UTF-8读取后逐行逐字段比较，全部相同；含Q1温度/水分、Q2温度/水分、Q3水分、Q4水分及配套半径。共54个数据行，含空白位置、显示时刻、表面与半径，未将Q4动态表面改成固定2cm。

| 绘图数据 | 场数组形状 | 数值及NaN位置比较 |
|---|---:|---|
| Q1温湿剖面 | 7×121 | C、温度、半径、时间全部相同 |
| Q2温湿剖面 | 6×121 | C、温度、半径、时间全部相同 |
| Q3干燥图 | 266×121 | C及中心/最大/均值/表面/R/坐标/时间全部相同 |
| Q4收缩干燥图 | 360×121 | 同上，域外NaN位置也相同 |

所有可比较数值数组最大绝对差0。本项比较绘图数据，没有把PNG/PDF容器哈希差当作数据差，也没有再次进行视觉审查。正式四张图的可读性已经由独立图审查记录，GUI进程是否退出仍由VS实际观察记录。

## 5. 可复核产物

实际运行命令（从本届项目根目录执行）：

```text
C:/Python314/python.exe -B tools/compare_gui_final_v6a.py
```

工具只读取正式/GUI结果，写本审计JSON；本MD依据其真实结果整理。NumPy用于NPZ数组比较，标准库用于JSON/CSV、GZIP/ZIP、XML和哈希，没有导入冻结模型模块。

- 工具：`tools/compare_gui_final_v6a.py`；SHA256 `4695d92731729f43e3e8be487f676df4ae2ba414090d2056d0b8fd1006052f12`。
- 机器可读审计：同目录 `independent_comparison.json`；SHA256 `72ebd3a3f1ac9dc0975feabed9c7afab3866987beaec1fbd4b02e2dc80c98cd9`。
- JSON逐项保存了60个检查及结果、28项输入记录、三轨迹摘要和数组记录、全部GZIP解压哈希、XLSX解压条目清单/哈希、七CSV及四图数组比较、完成门文件哈希和状态。
- `failed_checks`为空；没有重跑、修补数值或覆盖任何生产/GUI输出。没有修改两份主MD、冻结物理推导或任何 `code/modeling/*.py`。

后续若任一源码、数据或数值产物改变，应按新版本重新界定本记录。当前PASS仅适用于表中这两次实际运行产物，不是人工审核或物理预测的通用认证。
