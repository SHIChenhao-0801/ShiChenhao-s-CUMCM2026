# A题有预算实验协议

开始核时：2026-09-10 13:43:14 UTC；整体工作目标截止2026-09-10 19:00 UTC（北京时间9月11日03:00）。用户已澄清 autoresponse 指 autoresearch，并授权本题自主实验。此处使用已安装适配器的建模实验方法，不运行上游语言模型训练。

## 固定目标与比较条件

本题没有内部温度/含水率实测标签，不能按虚构预测准确率挑选模型。数值方案的首要量为固定条件下Q3/Q4全域阈值时刻与更精细参考解的相对差；同时要求质量守恒、正性、边界符号正确，Q1热解通过Bessel独立解校核。成本以实际墙钟秒计。物理情景独立分组，不能把不同边界下更短的烘干时间叫作算法改进。

基线采用圆柱一维径向有效显热及干基水扩散。Q1使用附录2；Q2/Q3全程附录3；Q4全程附录4，实测半径分段线性插值、同比材料收缩。经验密度仅作为有效热容量参数，不同时强制其等于骨架密度乘(1+C)。这一闭合理由、误差和替代情景在完整推导中说明。

## 批次

1. 基线：保留输入SHA256和完整脚本；先粗网格取得实际解，失败修复须留记录。
2. 数值比较：固定同一物理条件，N=50/100/200/400逐级比较；必要时加N=800作独立终检。单独收紧时间容差/最大步长，不把输出采样间隔当积分步长。
3. 物理稳健性：4小时后环境平台值和尾段延拓；传热/传质系数情景；固定半径与收缩在同一附录4下对照；端面与潜热只作为清楚定义的替代模型，参数来源不足时不冒充识别结果。
4. 输出验证：题给空间/时间网格回填和四位小数；未达标不能输出伪终点；移动边界外位置留空并单独给当前表面，不外推材料场。
5. 冻结：保留最佳有效数值方案及所有失败/舍弃记录，重新运行最终版本、哈希、检验；VS GUI复现和人工审查分开登记。

每批CPU串行或有限并行，不启动无限搜索。每个trial记录假设、配置、版本、实际开始结束时间、退出状态、指标与输出路径。不存在实测真值时不设置伪造的训练/测试集。对比结束后，以未参与调参的独立Bessel校核和更细网格作最终检查。到指定时点交付真实进度，不能用预期结果填补失败任务。
