# S6 正式运行与模型证据独立审查

审查对象：`final_v6a`。审查日期：2026-09-10 UTC，17:00起执行；机器可读报告记录确切生成与冻结时间。结论：**S6 official evidence gate 和 workflow guard 均已实际通过**；四问正式运行契约、指标来源、图表输出、版本对应关系，以及已完成的最终同网格时间检查有可复核证据。本结论的范围是成稿前的运行与结果证据，不能代替真实药材预测精度、Visual Studio GUI 验收、用户人工代码审查或正式论文/提交终审。

机器可读报告：`s6_modeling_evidence_review.json`，SHA256 为 `9ca99955b90d8837e1b2477a6a7438f7dd9ce3fddf60caa7ab98df71c964cad5`。其中按10个公开的运行证据检查项计算通过率100%；这个数不是比赛得分、模型预测准确率或全部交付完成率。

## 1. 实际门禁操作与范围

在比赛子工作区，以 `C:/Python314/python.exe -B` 顺序执行：

1. `workflow_guard.py --status`：确认真实当前为 S5、下一步 S6。旧 workflow_memory 中的 S4 记录已落后，未凭旧记忆继续。
2. `workflow_guard.py --skill quality-assurance-auditor`：实际 PASS，满足该技能入口要求。
3. `quality-assurance-auditor/scripts/evidence_gate.py --mode official`：实际退出码0、PASS，无失败或警告。
4. `workflow_guard.py --step S6`：S0—S6 实际全部 PASS；报告冻结快照为 `s6_modeling_evidence_review.guard_snapshot.json`。
5. 完成审查后再执行 `workflow_guard.py --status`：当前 S6、下一步 S7、推荐 `paper-formal-writer`。只读取推荐，不调用后续写作；快照为 `s6_modeling_evidence_review.workflow_status_snapshot.json`。

正式技能输出为 `paper_output/qa/evidence_gate_report.json` 和 `.md`。JSON 的 SHA256 为 `569a3a42937ca7b1c3261e4537feb429c0d443d652973c4760584c14a0be4536`，记录了本次七份输入契约的真实 SHA256：模型路线、图索引、模型结果、运行账本、指标、结论、表索引。审查重新核对了这七个哈希。

该 official 脚本核对每问的结果摘要、非空有限指标、结论文本、图表/表格存在性及非占位状态、源代码/运行记录和输入输出哈希；**它没有检查论文排版、实验真值、最终时间误差、GUI 或人工审查**。本报告把后几项另列，不将脚本的 PASS 扩大解释。

## 2. 四问模型与实际生产契约

四问均为真实 A 题的前向机理求解，没有出现替代题或通用趋势代理。Q1 使用附录2的预热模型；Q2/Q3 从 t=0 使用附录3并共享 `final_v6a:Q23`；Q3 是该轨迹上全域含水率的严格阈值任务。Q4 从 t=0 使用附录4整组物性，并加入附件2给定半径下的固定长度同比材料收缩。Q2→Q4的差异同时包含物性与几何变化，不能全归于收缩。

| 问 | 正式轨迹/空间区间数 | 生产契约情况 |
|---|---|---|
| Q1 | Q1 / 3200 | 0—1800 s逐秒、21个固定物理半径的温度和含水率；正文7时刻×5位置 |
| Q2 | Q23 / 3200 | 完整0—206902 s逐秒、两张工作表各206903行；正文前三小时的6时刻×5位置 |
| Q3 | 同一Q23 / 3200 | 全域临界事件、每60 s完整输出及临界/事后终点；正文每6 h与严格报告终点 |
| Q4 | Q4 / 6400 | 动态物理域，域外留空，实际表面独立列，半径独立工作表；正文实际表面与固定厘米列明确区分 |

实际模型结果有4条、指标14条、结论4条、正式正文CSV7份；图索引保留4张计算主图和2张观测输入图。official report 中 Q1/Q4各有2图，是对应观测图加计算图，没有把观测图冒充数值解。

本次独立读取全部14条指标的 `evidence_json_pointer`，确认值为真正有限数值、与落盘 summary 中的原值精确相等，且证据文件已进入正式运行账本。Q1/Q2结论中的温度和表面含水率另有完整正文CSV及图数据一致性证据。

三条生产轨迹的离散质量残差最大值分别为 Q1 `5.773159728050814e-15`、Q23 `1.1546319456101628e-14`、Q4 `7.993605777301127e-15 kg/kg`；求解均成功、零警告、物性正、含水率非负。初始C的极微小上偏约1e-15属于浮点量级，不采用裁剪来伪造正性。

Q3连续临界时刻为57.47230195056044 h，向上报告 **57.4724 h**，原精度 max C 为0.14999989718225315；Q4连续临界时刻51.09057478683054 h，向上报告 **51.0906 h**，原精度 max C 为0.14999995339076624。显示0.1500本身不证明严格达标，末行数值是在真实报告秒数206900.64和183926.16处查询。

## 3. 解析、空间与时间证据的确切版本

历史数值检验并非全部用当前v6重新运行。本次同时核对保存的旧源码与实际场样本，建立可以复核的对应关系：

- v2 Kirchhoff 起的 `radius`、`environment`、`properties`、`water_internal_flux`、`rhs`、`initial` 六个方程相关方法，与当前v6的AST完全相同。
- v3/v4/v5至v6还改动了 Jacobian接入、Run生命周期、诊断归约和连续输出存储等实现；只把明确相同的方法称为相同，不宣称整份代码相同。
- Q1历史v3 N3200、Q23历史v5 N3200、Q4历史v5 N6400的 `sampled_solution.npz` 与正式v6a对应文件均逐字节相同，所有保存数组也分别比较为零差。
- 保存样本的一致性不单独证明所有连续时空点一致；v6另有三条完整N40轨迹的memory/disk实际身份检验，包括accepted断点及其左右相邻浮点时刻，状态/事件/连续查询均零差。这是存储一致性证据，不是高网格空间误差证明。

最细相邻空间比较如下，均为实际整数秒×21个指定物理半径的比较；Q4只比较双方域内有限点：

| 模型 | 网格 | 最大温度差/K | 最大含水率差/(kg/kg) | 连续事件细−粗差/s |
|---|---|---:|---:|---:|
| Q1（历史v3） | 1600→3200 | 1.916171868e-6 | 4.690332349e-5 | 不适用 |
| Q23（历史v5） | 1600→3200 | 2.892925920e-6 | 3.830289061e-5 | −0.02014642893 |
| Q4（历史v5） | 3200→6400 | 9.633932905e-7 | 1.979947177e-5 | −0.004170547734 |

Q1常系数热方程可以与Bessel/Duhamel解析级数比较；非线性含水率没有因此获得相同解析真值。采用独立常数D=4.938e-9的水分基准，N1600/N3200在全部1801个整数秒×21半径上的解析最大水分差分别为6.192999926e-5和1.544866339e-5 kg/kg。早期N400报告的 `REVIEW_REQUIRED` 真实保留；其最早阶段水分误差未通过原阈值，后续细网格基准提供了补充证据，不能倒写旧报告为PASS。

本轮最终同N时间检查已经全部完成。baseline使用rtol=1e-10、T atol=1e-10、C atol=1e-12、早期最大步2 s、后段最大步120 s；tight分别收紧一数量级，并将最大步改为1 s、60 s。6个实际子进程全部退出0、零警告、Run关闭；每批基准保存样本与正式生产逐字节相同，记录的输出哈希已独立核对。

| 模型 | N | 公共整数秒数 | 最大温度差/K | 最大含水率差/(kg/kg) | 事件tight−baseline差/s |
|---|---:|---:|---:|---:|---:|
| Q1 | 3200 | 1801 | 1.627586983e-6 | 3.619134326e-9 | 不适用 |
| Q23 | 3200 | 206903 | 2.660259952e-6 | 9.941051204e-9 | +3.975728760e-6 |
| Q4 | 6400 | 183929 | 9.443490399e-7 | 9.818015290e-10 | −3.415538231e-6 |

这些量是相邻空间网格或时间控制的数值敏感性，不能称为严格连续解误差上界或实测预测精度。时间容差与最大步同时收紧，因此不把该试验拟合成单独时间步的收敛阶。经验Richardson指示量及其渐近假设由独立数值审计另行解释。

## 4. 图表和体积证据

本代理先前已完整目视四张PNG及四份PDF的实际Poppler渲染；中文、单位、图例、移动域空白均通过。七份正文CSV共297格具备逐格live Run重新查询验证；四工作簿共9335598个单元格已有真实完整生产回读。详见 `final_visual_review.md/.json`，本次引用并核对其源文件哈希，没有将文件存在误当视觉检查。

四个原样XLSX总30218400字节，93文件现有数值支撑候选实际ZIP为14382321字节，全部条目解压哈希一致。规定的逐秒/厘米点没有裁减。候选没有自动纳入内部17位全量档案，但这些档案仍留在正式生产目录；完整四位结果工作簿全部保留。加入AI详情PDF和最终复现说明后仍须实测最终包。

## 5. 假设和数据支持边界

以下限定已经写入正式 `assumptions_used` 和 `limitations`，后续主稿必须保持：

1. 题给rho×cp作为有效体积显热容量，干物质质量另行守恒，不同时宣称经验rho仍是真实湿质量密度。题给密度、固定长度、全部半径观测和干质守恒不可不加说明地照字面强拼。
2. 附件空气kg/kg直接用作材料干基等效Ceq，是从t=0就引入的闭合约定，不是由附件证明的气固吸附平衡换算。
3. 环境观测止于4 h；之后50°C/0.05属于延拓假设，真实干燥事件位于该假设区间。半径来自附件观测，本模型没有预测它。
4. 不显式潜热的基线和表面全量潜热负荷情景各有清楚含义。后者在既定等效质量驱动力下可能与湿空气平衡不相容，只作为条件性压力测试，不冒称更真实温度或严格上界。
5. 端面二维对照、参数/尾边界情景和同附录4物性固定/收缩对照均保留各自网格、版本和适用范围；不能从粗网格β反向趋势推导最佳工艺，也不能用跨问时差冒充纯收缩效应。
6. 没有内部T/C观测真值，不报告实测RMSE、训练/测试准确率、统计置信区间或参数校准成功。101个已登记公式块是本次文档口径，包含定义、推导和算法，不等于101条独立物理定律；题给9条经验函数的拟合系数不能由缺失原实验数据重新推导。

## 6. 剩余事项与交回主代理

本报告没有将以下事项标为通过：

- 主代理正在进行的Visual Studio正式GUI复现：需独立记录源码、断点/变量与实际执行/数值比对证据。
- 用户/团队对模型闭合选择和核心代码的人工审查；AI审阅不能替代。
- 最终AI工具使用详情PDF、环境和复现说明、完整支撑包匿名性和最终大小/哈希验收。
- 正式论文的S7/S8：章节内容、原生公式、正文引用、图表编号、排版和逐页渲染检查。本子任务没有触发写作。

S2计划中的 `fig_q1_1/fig_q1_2` 等为设计期名称，实际主图合并成 `fig_q1_profiles` 的两子图，其他问同理。写作时以最终figure_index的真实ID为准，必要时在正文说明子图映射，不能照抄不存在的预定路径。原计划文件未为了消除历史状态而改写，避免破坏已冻结的运行输入哈希。

按照主代理分工，主memory和workflow_memory没有由本代理修改。请主代理使用context-memory-keeper记录S6门禁、报告哈希、最终时间检查完成事实和上述剩余项。任何修改七个正式证据门禁输入的行为都会使现有fresh PASS失效，应重新执行official门禁及guard。
