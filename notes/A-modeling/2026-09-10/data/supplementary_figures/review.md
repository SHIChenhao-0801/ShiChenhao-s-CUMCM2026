# 补充图：来源、绘图数据与视觉复核

状态：**PASS，范围为已冻结证据重绘、数值回读和 AI 视觉检查**。三张 PNG 及对应三份单页 PDF 的 Poppler 渲染均已完整目视；标题、轴与单位、刻度、中文字体、图例、条形末端数值、页脚均清楚，没有裁切、重叠或缺字。三份 PDF 的微软雅黑及粗体字形均已嵌入。本次没有新的 PDE 求解，没有改动正式 `code/modeling/*.py`、正式图索引或 S0–S6 门禁输入。新增驱动的 Visual Studio GUI 复现及用户人工审查仍待完成，不把本次静态/命令行检查冒充这两项。

## 可直接嵌入两份主 MD 的链接与图注

以下链接相对于 `notes/A-modeling/2026-09-10/` 下的两份主 MD。此审查文件仅给出链接，不修改主稿。

```markdown
![全整数秒与共同物理半径采样上的空间网格差](data/supplementary_figures/fig_model_grid_convergence.png)
```

建议图注：采用已完成的相邻网格比较，逐整数秒在 21 个固定物理半径上求最大含水率差，第四问仅比较同时位于材料域内的有限值。第一问比较 N=800/1600/3200，第二、三问此图展示最终 N=1600/3200 对，第四问比较 N=1600/3200/6400。各问时间样本数分别为 1801、206903、183929；右图为细减粗临界时刻差的绝对值。Q1 证据来自 v3，Q23/Q4 来自 v5，均保留原始版本记录，版本衔接见数值审计。所画差值是数值收敛证据，不是严格连续解误差上界，也不是实测准确率；单个 Q23 比较点不单独支持观测收敛阶。

```markdown
![同附录4物性与参数下的收缩对照](data/supplementary_figures/fig_q4_shrinkage_control.png)
```

建议图注：采用已完成的同一 v2 源码、N=200 附录4物性实验，设置仅 `shrink` 开关不同。固定半径与给定收缩轨迹的临界时间分别为 129.8452283470 h 和 51.0909734198 h，时长缩短约 60.65%。这是有效模型中的几何情景对照，不能用第三问和第四问同时改变物性后的时长差来单独估计收缩作用；不把 N=200 对照值当作最终 N=6400 正式解。

```markdown
![环境观测结束后的尾边界情景](data/supplementary_figures/fig_q3_physical_sensitivity.png)
```

建议图注：第二、三问采用同一 v2 源码与 N=200，只改变 4 h 后的环境延拓。基准为 50 °C、等效平衡含水率 0.05；温度分别改成 49 °C、51 °C 时，其余参数不变，临界时间相对改变约 +3.25%、−3.11%。延续最后观测值及末 1 h 观测均值是另一种尾边界约定。均值情景在图中显示 +0.00% 源于保留两位小数，真实变化为 +0.0029529244%，并非严格零变化。情景变化不是统计置信区间，且不证明气相含水量至固相平衡含水率的映射已得到实测校准。

## 实际读回数值

### 全秒空间网格比较

| 问题 | N 粗→细 | 最大含水率差 / kg·kg⁻¹ | 细减粗临界时刻差 / s | 时间数 × 半径数 |
|---|---:|---:|---:|---:|
| Q1 | 800→1600 | 1.8988217883419978e-4 | 不适用 | 1801 × 21 |
| Q1 | 1600→3200 | 4.690332349133186e-5 | 不适用 | 1801 × 21 |
| Q23 | 1600→3200 | 3.830289061168557e-5 | −0.020146428927546367 | 206903 × 21 |
| Q4 | 1600→3200 | 6.976616569297905e-5 | −0.016721835534553975 | 183929 × 21 |
| Q4 | 3200→6400 | 1.9799471771719812e-5 | −0.0041705477342475206 | 183929 × 21 |

第四问最大差出现于边界收缩经过固定采样半径附近，不应解释为整个连续移动域处处的严格最大误差。原绘图函数的 NPZ 只记录左图含水率差和 N；本次另以 `convergence_plotted_values.csv` 保存全部绘图相关数值，包括右图事件差、极值时空位置、原始报告路径及哈希。

### 尾边界情景

| 情景，均 N=200 | 临界时间 / h | 相对基准变化 / % |
|---|---:|---:|
| 50 °C / 0.05 平台延拓 | 57.4727587858255 | 0 |
| 4 h 后温度 49 °C | 59.33996412900231 | +3.2488528176192633 |
| 4 h 后温度 51 °C | 55.68718955674412 | −3.106809672623112 |
| 4 h 后延续末值 | 57.169833881214096 | −0.5270756285430167 |
| 4 h 后延续末 1 h 均值 | 57.47445591294536 | +0.0029529244040427116 |

## 复核范围与文件

- 驱动 `build_supplementary_figures.py` 仅导入冻结的 `publication_plots.py` 三个读取历史证据的函数，不导入或运行求解器。实际运行退出码 0，三图合计约 8 秒，无新增 PDE 求解。
- 三组 NPZ 逐值与重新打开的来源 JSON 比较，全部精确一致。两份收缩设置的差异集合严格等于 `{shrink}`；五种环境情景只允许 `boundary_extension`、`tail_temperature_C` 变化，并验证相同网格、同源码版本。三份 CSV 留下全精度数值、来源路径及 SHA256。
- `generation_and_value_verification.json` 记录三张图的 PNG/SVG/PDF/NPZ/元数据哈希、全部来源与冻结代码哈希；生成前后输入与 `modeling/*.py` 均一致。它保留“视觉检查待办”的生成时事实，最终视觉结论以本文件与 `review.json` 为准，不回写生成期元数据。
- `pdf_structure_check.json` 记录三份单页 PDF 结构与字体嵌入；每图的 `_pdf_render.png` 是本次实际检查的渲染证据。
- 这些图是建模说明的补充材料，不在正式 `figure_index` 内，也不把它们追加为本次已冻结 S6 的门禁输入。没有执行正式论文生成或匿名提交包发布。

所有含水率和烘干时间解释仍以当前有效模型假设为条件；数值网格差、求解容差变化和物理假设情景应分别报告。
