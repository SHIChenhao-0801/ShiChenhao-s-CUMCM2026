# A题历史约束与截图计算来源核查

记录日期：2026-09-10。范围：`D:/Document/数学建模` 及其子目录的相关记录。新文件只写入 `2026CUMCM/notes/A-modeling/2026-09-10/data/`。没有修改全局记忆、应用聊天、原始会话、父目录或Git索引。

## 1. 当前建模必须继承的约定

1. 最新9月10日赛前说明会优先于旧模板和历史记忆；要求与建议分开，不把讲义内部冲突自行改成定论。
2. 2026正式结果和运行只以本届目录为准，2024/2025演练以及父目录旧`paper_output`不能计入本届PASS。
3. 用户主导关键模型、假设与结论；已有论文同学。交接必须含目标、数据处理、模型理由、符号/单位/推导、算法、真实结果、验证、局限和版本，不只给图片或代码。
4. 图、公式、表格要完整且互相一致，数量服务于论证。历史“运行越久越有奖/token越多越好”属于第三方聊天判断，不是质量判据；计算预算用于基线、误差、敏感性与复核。
5. 任何数据或物理模型改变，相关结果都须重新计算、验证并更新正文。未实际运行不能写成已求解，收敛通过不能当作实测准确。
6. 比赛Python/C++若实际采用，须Computer Use在Visual Studio中可视化复现，核心代码待用户人工审查；Node读表也不等于已完成VS复现或用户审查。MATLAB/R小样通过不等于本题通过。
7. 本机按CPU和有限预算推进；不把原版autoresearch的NVIDIA GPU训练当必需项。不以下载/安装成功替代依赖和正式功能验证。
8. 主代理统一Git备份与结果版本；子代理不并发Git。正式论文匿名、PDF与支撑包体积、AI声明与支撑详情均按本届要求执行。

这些结论来自本届`AGENTS.md`、`memoryskill.md`、父目录聊天主题索引、9月9日视频学习笔记、林宇翔建模观点笔记及论文协作方案。老文件中“用户独自负责全部人工工作”“MATLAB仍待核实”等已被新记录覆盖，本次不沿用。

## 2. autoresponse 的直接历史证据

按每个JSONL首条`session_meta.cwd`核对，只读取本项目及子目录会话正文；2026-09-10目录有33条匹配记录。完整有限范围命中列表见本目录`project_history_matches.json`。只提取用户和助手的可见文字，没有导出内部推理或无关其他工作区正文。

会话`01a08b70-beb1-71b3-9b9e-7a9d83bf114f`中：

- UTC 2026-09-10 13:24:24（北京时间21:24:24），用户询问“autoresponse那个skill怎么调用”。
- UTC 13:25:41，助手报告已核实当前目录不存在名为`autoresponse`的技能，实际已安装技能叫`autoresearch`，并给出`$autoresearch`调用方式。
- 该回复解释的用途是基线→单次改动→真实运行→固定标准比较→保留/回退→记录。对A题优先研究数值收敛、效率和合理假设敏感性，不能无内部实测却优化所谓预测准确率。回复引用本地适配规则默认无预算时按30分钟一批。

本轮用户继续使用autoresponse称呼，应与上述紧接的上下文一起解释；是否采用实际`autoresearch`由主代理结合当前Skill文件执行，不需把这个历史误称当作全新未知插件。

## 3. 截图里N=400/Bessel/仿射收缩的来源

用户截图显示4项完成、2项进行中、4项待处理，内容涉及标准库xlsx读写、四问统一模型、Bessel常系数基线、50/0.05延拓、N=400后台生产计算和Richardson/守恒/端面检验。这是用户提供的任务状态画面，可作为待对照的方法清单；其中勾选状态不独立证明本工作区已有对应代码、数值日志与校验。

本次已完成的有限范围查找：

- 在本届`notes`、`tmp`、`paper_output`及父目录相关`notes`、历史`paper_output`搜索`N=400`、`N = 400`、`Bessel`、`仿射收缩`、`有限体积`、`生产计算`。
- 找到本届A题原则讲解、计划和可行性评估中讨论有限体积等候选；没有找到与截图生产求解对应的实现、结果或日志。父目录少量论文文本命中属于历史文献。
- 在当前对话开始前、2026-09-10本项目会话可见正文中，未找到`N=400`、`Bessel`、`仿射收缩`、`生产计算`的历史命中。排除了当前任务及子代理，避免把本次搜索语句误当旧证据。截图中的文字本来可能没有可检索的转写，因此“未命中”不证明截图来源不存在。
- 只读进程列表中，带本届目录/求解关键词的可识别程序只有A题PDF阅读器与历史B模拟器，没有识别到正式A求解的Node/Python/MATLAB/Rscript进程。进程名称和命令行检查不能排除远端、其他软件内部或没有关键词的计算。

**本次结论：截图对应的生产代码/运行证据尚未定位，不能声称其Q1–Q4已运行或检查已通过。本子任务没有另启PDE生产计算，也没有终止任何进程。** 主代理如另行建立数值实验，应使用新的可追溯输入、代码、参数、运行证据与状态，并保留截图为用户参考材料。

## 4. 与后续建模直接有关的历史待解项

本届`memoryskill.md`及A原理讲解已指出：空气kg/kg与药材干基的边界对应、潜热处理、端面影响、密度/干物质守恒/收缩速度相容性、4h后环境延拓，以及Q2完整结果终点。它们仍须靠本轮模型论证与验证解决，历史指出问题不表示问题已解决。

本次新增的代数审计进一步证明：若附录4密度当精确湿体积密度，同时固定长度、采用72h半径1.198cm、C≥0并要求干质量严格守恒，会发生数值相容性冲突。详细计算见同目录`data_audit.md`第5节。该发现应进入模型选择理由和局限，不能由论文润色掩盖。

## 来源入口与阅读限制

- 本届：`AGENTS.md`、`memoryskill.md`、`notes/A-plan/2026-09-10_A题执行计划.md`、`notes/A-principles/A题_公式推导与原理讲解.md`、`notes/selection-evaluation/2026-09-10/A题_可行性评估.md`。
- 父目录只读：`notes/chat-history/README.md`、`decisions-and-open-items.md`、`notes/modeling-prep/2026-09-09-bilibili-learning.md`、`2026-09-09-wechat-linyuxiang.md`、`2026-09-09-paper-collaboration.md`。
- 当日会话原件按`session_meta.cwd`筛选，结果索引存于`project_history_matches.json`。

旧索引只涵盖当时本地数据库登记的会话；当日追加搜索只覆盖已存在的本地2026-09-10 JSONL及其可见文本。未声称逐条读完所有截图、语音、云端和其他设备历史，也未将第三方私聊中的话作为新操作授权。
