# final_v6a 正式图表独立审查与支撑包体积预检

审查日期：2026-09-10 UTC。结论：**四张正式主图的 PNG/PDF 视觉显示、七份正文 CSV 的表达与正式回读证据均通过本项审查**，未发现需修改冻结模型或图脚本的问题。状态为 `PASS_FOR_FOUR_MAIN_FIGURES_AND_TABLE_PRESENTATION`。本项结果不替代模型预测精度、Visual Studio GUI 复现、人工审查或最终提交终审。

来源为 `paper_output/results/production/final_v6a`。生产配置：Q1/Q23 径向区间数 3200，Q4 为 6400；CPU 单线程；BDF、Kirchhoff 面通量、解析 Jacobian、磁盘保存的原精度连续解。生产进程记录为 PID 32364，实际退出码 0，完成于 UTC 16:31:52。审查没有修改该目录及 `modeling/*.py`，也没有操作 Git 或 GUI。

## 1. 实际查看的图像

四个 PNG 均完整目视；四个 PDF 均使用本机 Poppler 重新渲染成 PNG 后完整目视。每个 PDF 均为一页；pypdf 只读检查显示 Microsoft YaHei 常规和粗体字体均已嵌入。视觉证据保存在 `data/final_visual_assets/fig_*_pdf_render.png`，结构与字体证据为 `pdf_structure_and_fonts.json`。没有用 PDF 文本抽取替代视觉检查。

| 图 | 实际检查内容 | 结论 |
|---|---|---|
| `fig_q1_profiles` | 预热阶段温度/干基含水率双图；距离 cm、温度 °C、含水率 kg/kg；100、300、600、900、1200、1500、1800 s 七条曲线；脚注列明有效模型及边界约定 | 中文、刻度、图例和脚注清晰，没有缺字、遮挡或裁切 |
| `fig_q2_profiles` | 标题明确前三小时；0.5、1、1.5、2、2.5、3 h 六个时刻；摄氏温度与干基含水率、物理半径单位正确 | 图示范围与题目正文表一致；不把前三小时的图当作完整结果终点 |
| `fig_q3_drying` | 固定物理半径 0..2 cm、全时段小时轴的含水率热图；中心/干质量加权均值/表面轨迹与 0.15 kg/kg 阈值；临界时刻箭头；4 h 分界和后续边界延拓说明 | 图形清晰，单位正确。中心曲线最晚达到阈值，脚注明确这是条件性有效模型 |
| `fig_q4_drying` | 使用物理距离而非材料索引；实际表面 R(t) 从 2 cm 收缩至约 1.2 cm；曲线上方完全留白并标明“域外（非药材）”；中心/均值/实际表面轨迹、阈值与临界时刻 | 域外没有填成零水分，表面含义清晰，中文、单位和图例显示正常 |

另核对了绘图数据本身：Q3 为 266 个绘图时刻×121 个物理半径；Q4 为 360×121。Q4 的 15648 个域外样本全部为 NaN，域内全部有限，实际半径与附件观测的分段线性插值零差；Q3 半径恒为 0.02 m。各绘图时刻满足中心≥干质量均值≥表面（容许 1e-9 数值舍入量），全域最大值与中心一致。PNG/PDF/SVG、绘图 NPZ、metadata 及其记录的源码和输入哈希均匹配当前原件。

Q1/Q2 的图像数据在正文指定的五个物理半径和所有规定时刻上，与正文 CSV 逐值比较，均只存在四位小数舍入误差，最大不超过 5e-5。证据为 `final_visual_assets/q1_q2_plot_table_consistency.json`。

当前独立图像显示通过，不等于已在未来论文版面中缩放后逐页验收；正式排版阶段仍需检查最终字号与可读性。

## 2. 正文 CSV 与导出报告

本次完整读取七份 CSV：Q1/Q2 各温度和含水率两表、Q3 含水率一表、Q4 含水率和半径两表。四问的七表共 297 个时间/数值/空白单元格，均已有生产过程从 live Run 独立重新查询的逐格验证，审查核对了 PASS 状态、时空口径、全部表值和文件哈希。

| 问 | 正文表网格 | 正式 XLSX 数据行数（每表） | 实际完整回读单元格 |
|---|---|---:|---:|
| Q1 | 7 个秒时刻×5 个物理半径，温度和含水率 | 1801 | 79244 |
| Q2 | 0.5..3 h 每 0.5 h×5 个物理半径，温度和含水率 | 206903 | 9103732 |
| Q3 | 每 6 h，加实际向上报告终点；5 个物理半径 | 3451 | 75922 |
| Q4 | 每 6 h，加实际向上报告终点；5 个固定半径、独立实际表面与半径表 | 3068 | 76700 |

正式工作簿累计 **9335598 个单元格**已在生产中完整流式回读，和未舍入归档的四位小数投影一致；各问另有 14 或 15 个 live Run 时点样本验证，最大未舍入差为 0。这里审阅了真实生产验证报告，没有重新求解 PDE，也没有声称本子任务再次执行了九百余万单元格的完整回读。

Q2 从 0 s 到 **206902 s** 逐秒输出，共 206903 个时点，两张表均完整；没有只保留前三小时。Q3/Q4 工作簿每 60 s 保存，并加入连续临界事件和事后核验终点。

严格达标与展示时刻分别为：

| 问 | 连续临界时刻/h | 向上报告时刻/h | 报告时刻/s | 报告时刻未舍入 max C | XLSX 事后核验末秒 |
|---|---:|---:|---:|---:|---:|
| Q3 | 57.47230195056044 | **57.4724** | 206900.64 | 0.14999989718225315 | 206902 |
| Q4 | 51.09057478683054 | **51.0906** | 183926.16 | 0.14999995339076624 | 183928 |

图中的 57.472 h 和 51.091 h 标注为“全域临界时刻”，采用三位小数作图示；正文末行应采用表中的向上报告时刻。显示为 0.1500 本身不是严格 C<0.15 的证据，严格判定由未舍入值提供。

Q4 正文每个采样时刻的 1.5 cm 和 2 cm 两列均在药材之外，七列表的 18 个域外单元格正确留空，独立表面列有数值。半径表与含水率表同时间；例如 6 h 时 R=1.3740 cm，含水率表仅固定 0、0.5、1 cm 三点有效，表面 C=0.4207 位于 1.3740 cm，绝非 2 cm。全量 Q4 工作簿的域外空白总数为 23173。

## 3. 实际体积和无损候选

| 原样结果文件 | 字节 |
|---|---:|
| result1.xlsx | 390899 |
| result2.xlsx | 29210086 |
| result3.xlsx | 339137 |
| result4.xlsx | 278278 |
| **四文件合计** | **30218400** |

单独完整 `result2.xlsx` 通过标准 ZIP 的 DEFLATE level 9 外层压缩，实际得到 **10109027 字节**；解压后的 SHA256 与原工作簿一致。原工作簿未重写、没有删除任何规定时间点或空间点。此数据存在可再次压缩的重复结构，不能仅凭“XLSX 已压缩”推定外层压缩一定无效。

对完整的现有数值材料进一步实测，候选 `final_visual_assets/support_preflight_existing_numerical_materials.zip` 共 **93 个文件**，包含四份原样结果工作簿、全部冻结生产输入和模型源码、四张主图的 PNG/SVG/PDF 与绘图数据、七份正文 CSV、运行/验证/契约证据。解压内容总计 **34717917 字节**，压缩包实际 **14382321 字节**，相对于 20000000 字节暂余 **5617679 字节**。包内每个条目均流式解压核对 SHA256，与源文件逐字节一致，并复核所有源文件未改。

此候选是现有数值材料的体积预检，**不是最终可提交支撑包**：尚需正式 `AI工具使用详情.pdf`、最终环境/复现说明和人工/GUI验收记录，且候选保留内部证据路径，未经最终匿名性审查。加入所需材料后必须重新打包、实测字节并逐项核对。

17 位有效数字的全量内部输出归档仍保留在生产目录，但按已定交付策略不自动纳入限制体积的支撑候选；规定的四位小数完整工作簿全部保留。候选也没有收入往年资料、原始聊天、Git、临时 BDF 缓存或无关历史实验。

候选 ZIP SHA256：`f25729fa3f9f01c5a97cdc07b7bd6d178040dc19f3a14555ef22022732479e4e`。完整文件清单、字节与哈希、解压一致性见 `final_visual_assets/support_preflight_existing_numerical_materials.json`。

## 4. 审查证据与边界

本报告的机器可读对应件为 `final_visual_review.json`，SHA256：`e151b6556941e8674a622edf1d91941bdc9df8d87388c088178446b7c61e0147`。其中逐图记录了 PNG/PDF 和实际渲染图哈希、人工式 AI 视觉观察、数据几何检查；逐表记录首末行、空白数、完整正式回读报告路径及纸面报告时刻；另记录字体嵌入、图表数值一致性和支撑体积证据。

四位小数是题设输出契约，不是经验模型预测精度。当前数据没有药材内部温度/含水率实测真值；本检查只确认冻结的条件性模型结果被正确输出和表达。后续模型、输入、源码或任何正式数值变更，均应重新生成并复核对应图表。
