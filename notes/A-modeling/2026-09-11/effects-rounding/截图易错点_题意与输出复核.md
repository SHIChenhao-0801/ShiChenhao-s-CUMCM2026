# 四张“易错点”截图：题面、模板与当前输出独立复核

**结论：截图有实用提醒，但不能逐句当成题目指令。最需要修正的是Q2“完整烘干3小时”和“必须切换参数”，把21个输出半径称为全部求解节点，把60 s输出间隔称为必须的求解步长，以及把温/水两张sheet要求套到Q3/Q4。另有一项本轮新鲜模板读回的重要发现：原Q4模板末列就是“药材表面”，没有硬性规定固定2 cm列。当前0–2 cm固定列＋表面列属于明确的矩形输出展开约定，不能冒称原模板唯一格式。**

本报告接续同目录的七项核查与[四位小数专项复核](rounding-review.md)，不替代它们。四张参考截图均已实际目视；它们是待核查资料，不是新题设。本轮用pypdf 6.16.1重新抽取原PDF第1—3页，读取四个原始xlsx的实际表头、当前四份文件的实际sheet名/首行，另读取Q1末行与Q4六小时行。未重解PDE，未重复扫描Q2全部工作簿，未改任何冻结代码、正式结果、旧稿或Git/CUA。JSON记录原题/截图/源码/模板哈希与实际读回数据。

## 1. 原题与模板的实际要求

原题：`problem_files/CUMCM2026Problems/A题/A题.pdf`，4页，SHA256 `052d8014bff5727c019b72e44fdffaf5c145ce04050dd938baaf3527db331736`。

|题目|论文表要求|完整xlsx要求|原模板实际sheet|
|---|---|---|---|
|Q1|100、300、600、900、1200、1500、1800 s；0、0.5、1、1.5、2 cm；温度及浓度|1800 s内每1 s、每0.1 cm|温度、水分浓度|
|Q2|前3 h内每0.5 h；0、0.5、1、1.5、2 cm；温度及浓度|整个过程每1 s、每0.1 cm；题面未将完整文件截为3 h|温度、水分浓度|
|Q3|每6 h＋烘干结束时间；每0.5 cm；浓度|每60 s、每0.1 cm；浓度|Sheet1|
|Q4|每6 h＋烘干结束时间；每0.5 cm及真实表面；浓度|每60 s、每0.1 cm及模板末端“药材表面”；浓度|Sheet1|

PDF第2页Q2的决定性原文是“请建立整个烘干过程……的数学模型”，紧接括号“为简化问题，相关经验公式统一采用附录3中的公式”。同句后段“3 h内每隔0.5 h”限定的是论文中的表3/表4。背景“过程一般持续2–3天”不是让程序在精确48/72 h无条件停止的硬阈值。

原模板表头实际读取如下，省略号是原始单元格文本，不是本报告省略了某些真实列：

- result1、result2、result3：`时间\到药材中心的距离, 0, 0.1, 0.2, …, 2`。
- result4：`时间\到药材中心的距离, 0, 0.1, 0.2, …, 药材表面`。

PDF第3页的表6也写`0、0.5、…、药材表面`，没有明确列出固定1.5、2 cm。当前Q4正文表扩展为固定`0、0.5、1、1.5、2 cm＋表面`，完整表扩展为固定21半径＋表面，是覆盖各时刻可用位置的矩形并集网格实现；不能称为原模板强制列清单。

## 2. 逐项判断与代码证据

|截图位置及说法|判断|题意/当前实现证据|
|图2 II.1：附件1环境随时间变化，不能从头恒定60°C|正确|`dryingCore.py:96`起按附件实际时间插值；超过4 h才进入已声明延拓。这里的更新发生于求解器RHS评估，不必和xlsx输出时刻相同。|
|图2 II.2：论文7×5稀疏采样、完整文件1 s×0.1 cm|正确；“Excel是细密全部节点”需纠正|`exportOutputs.py:44`定义21个输出半径；`:234`起定义论文时刻和5个半径。`dryingCore.py:79`取N+1个求解节点，正式Q1 N=3200，所以是3201个空间节点，不是21个。|
|图2 II.3：输出四位，避免提前舍入|正确，附舍入规则边界|`roundFourDecimals`在导出时调用，求解状态保留浮点原精度；另存未舍入归档供复核不等于将正式表改为多位。ties-to-even/四位并非精度保证见前报告。|
|图2 II.4：Q1到1800 s|交付区间正确，“任何计算都不得超过”过度|`q1Model.py:7`、`dryingCore.py:333`实际终点1800 s；结果文件1801个含t=0样本。额外验证若明确为验证，不会因超出1800 s本身违反输出题意。|
|图2 III标题：“完整烘干3小时”|错误/易误导|原题区分整个过程模型和前3 h展示表；当前Q2完整文件到206902 s，绝非仅10800 s。|
|图2 III.1：预热后必须切参数|按“附录2→附录3分段切换”理解则错误|原文明确统一采用附录3。`q2Model.py:7`从t=0设置Q23；`dryingCore.py:126`附近依question分支使用附录3。物性随T、C连续更新，不是到1800 s才更换附录。|
|图2 III.2：论文时间h，Excel时间s|正确|`paperTables`把Q2论文时间除3600；`outputTimes`返回秒。实际Q2 A5402=5400，正文对应1.5000 h。|
|图3 III.3：不能直接复用Q1结果简单延长|正确；不应由此推出必须阶段拼接|Q2从均匀28°C、2.55重新算附录3的完整轨迹；`runDelivery.py:131`独立调用q2Model，`:166`起同一个Q23 Run导出Q2与Q3。Q1末状态不是Q2初值。|
|图3 IV.1：全域C<0.15，不能只看表面|正确|`dryEvent`及`completion`均取全部N+1浓度节点最大值。只看中心须先证明其始终为最大；当前代码无需这个替代假设。数值节点/插值场和连续真解的误差范围仍应区分。|
|图3 IV.2：论文6 h、Excel“步长”60 s|采样要求正确，“步长”需限定|60 s是输出采样间隔，不是求解器必须采用的固定时间步。当前BDF自适应积分、连续输出查询；Q3/Q4另保存事件和复核终点行。|
|图3 IV.3：不能设固定结束时刻，必须写循环等全部达标|核心目标正确，算法限制过强|连续事件机制同样有效，不要求手写for/while逐60 s扫描。`dryingCore.py:333`设置240 h搜索上限，`:343`附终止事件，`:353`定位临界根，随后继续积分复核。若上限内无事件，`completion`报错；上限不是伪造完成时长。|
|图3/4 V.1：半径变化，不能继续把物理域固定为2 cm|正确；不能扩张为禁止固定材料网格|当前R来自附件2，`x=r/R(t)`网格固定而物理r随时间移动；`:90`、`:79`及`:234`体现几何与查询映射。固定x数值网格与固定物理半径域不同。|
|图3/4 V.2：不能只改最大半径、忘记内部映射|提醒正确|`fields`将固定输出r除以当前R后查询；RHS具有当前R²尺度。材料收缩速度与守恒假设的物理审查交由物理分支，本报告不把截图当作这些闭合已成立的证据。|
|图4 V.3：末端是当前表面，“不要一直写到2 cm”|真实表面解释正确；输出列解释须细化|原模板确实以“药材表面”结尾。但当前固定0–2 cm＋表面列只在r≤R处填值，域外空白；这不代表材料仍到2 cm。不要逐行改变固定列的物理含义，也不要把固定2 cm列冒充当前表面。原模板没有要求必须保留固定2 cm，不能反向杜撰此硬要求。|
|图4 VI.2：初值28°C、2.55且全内部均匀|正确|`dryingCore.py:29`及`:199`将所有节点初始T=301.15 K、C=2.55，所有独立题目模型共用该初始状态。|
|图4 VI.3：A列秒，第一行为距离；所有题两张温水sheet|前半正确，后半错误|两张温水仅Q1/Q2。Q3原/现均浓度Sheet1。Q4原模板仅浓度Sheet1；现新增辅助“半径”，两张表也不是温度/浓度。|

代码行号对应本轮已读取并记录哈希的 `paper_output/code/review_delivery/` 副本。这里没有对图1物理方程/轴向省略或图4 VI.1一般稳定性作越权的最终判断，相关完整物理审查由并行分支提供；本表覆盖分派的题意、模板、初值、采样及交付解释。

## 3. 本轮实际读回的关键证据

当前CLI交付目录为 `paper_output/results/code_delivery/camel_final_v2/outputs`，已与原生产建立数值对应；本轮重新读sheet结构和选定行，完整行数取实际导出契约，不假称又遍历了所有933万格。

|文件|实际sheet名|数据行数/每表|列数|输出时间|
|---|---|---:|---:|---|
|result1.xlsx|温度、水分浓度|1801|22（时间＋21半径）|0–1800 s，每1 s|
|result2.xlsx|温度、水分浓度|206903|22|0–206902 s，每1 s|
|result3.xlsx|Sheet1|3451|22|正常每60 s＋临界事件＋206902 s复核终点|
|result4.xlsx|Sheet1；半径|3068；3068|23；2|正常每60 s＋临界事件＋183928 s复核终点|

Q1温度实际第1802行：A1802=1800，B1802=33.5753°C，V1802=36.7856°C。当前Q1求解有3201个空间节点，这21个物理位置只是题目完整输出网格；“完整”指没有漏掉题目要求的采样位置/时刻，不要求把全部内部求解节点写入题面工作簿。

Q2的5400 s关键格沿用刚完成的独立读回证据（本轮未无理由重复）：A5402=5400 s，B5402=45.8468°C，V5402=47.1401°C。论文时间是1.5000 h。完整文件206903行的事实在本轮读取的`result2.export.json`中有首末时间及行数记录，生成代码也明确用`np.arange(int(round(end))+1)`，没有在3 h截断。

**Q4六小时行本轮新读回**：两个sheet的第362行均为21600 s。半径sheet的B362=1.3740 cm；浓度Sheet1在固定1.3 cm处仍有0.5650，而1.4 cm至2.0 cm各列为空，末列W362“药材表面”=0.4207。表面位于1.3740 cm，不是固定2 cm；这是现有矩形网格正确保留域外空白的直接例子。

对应正文Q4第一行：6.0000 h，在0、0.5、1 cm处分别1.7196、1.5377、1.0226；1.5和2 cm空白；表面0.4207。额外半径CSV给1.3740 cm。若将这一行1.5 cm或2 cm空列直接改名为1.374 cm，会改变整列坐标契约，也破坏后续跨时间比较；需要新定义输出结构而不是只改表头。

Q3/Q4正文末行继续采用四位严格报告时间57.4724 h/51.0906 h；四位显示0.1500与原精度严格不等式的区别见四位专项报告。本轮没有将其改为最近舍入临界标签，也没有变动任何原有数据。

## 4. 建议并入总答复的修正文案

> Q2从t=0统一采用附录3模型，初值为均匀28°C和2.55 kg/kg；它不接续Q1的末状态。前3 h仅是论文展示范围，完整文件按每1 s覆盖同一Q23轨迹至干燥复核结束。Q1/Q2每0.1 cm的21点是题目规定输出点，不是全部求解节点；Q3/Q4每60 s同样是输出采样，不是BDF的固定步长。Q4在实际移动域内求解，固定物理半径的输出点越过表面后留空，并另列真实表面及其半径。

> 原Q4模板末列为“药材表面”，未硬性要求固定2 cm列。当前矩形输出保留0–2 cm固定位置并另加表面，只是一种已验证、明确标注域外空白的实现；不能把2 cm等同收缩后的表面，也不能把该实现反说成题面的唯一列布局。原模板Q1/Q2分别有温度和水分两张sheet，Q3/Q4只要求浓度；Q4新增半径sheet是辅助信息。

若论文阶段重新设计Q4表格，应按原题“固定距离间隔＋真实表面”说明范围、单位、域外空白与辅助半径，不需要为这轮截图直接修改已经冻结且含完整数据的正式输出。是否在最终比赛支撑文件中保留额外半径sheet，应在最终模板/提交检查中明确记录其辅助性质，不能说原模板本来就有此sheet。
