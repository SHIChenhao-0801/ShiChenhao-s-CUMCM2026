# B 题模拟器可用性实测记录（已结束）

日期：2026-09-10，北京时间约 20:57。最终状态：用户随后明确选择 A 题、不再考虑 B；不继续 B 的测试或开发。

## 范围与实际过程

用户要求用 Computer Use 检验 B 模拟器，同时评估 A，起初选题待定。通过 computer-use 的 `@oai/sky` 实际启动本届目录下的模拟器，并确认 v1.1、已连接、已登录。登录由用户完成，代理没有填写认证表单。通过 GUI 点击“开始问题3演练测试”，在界面显示“等待机器狗进入”后，使用附件 2 第 10 节动作序列做本机 HTTP 协议验证。

GUI 操作由 Computer Use 完成；HTTP 请求由 [b-simulator-smoke.ps1](../../../tools/b-simulator-smoke.ps1) 发出。这是接口连通性和计时检查脚本，不是搜索定位算法，不是 MATLAB/VS 复现记录。队号仅作为运行时参数，保存的源码与证据已脱敏。

## 已验证的结果

- 问题 3 演练案例：`RCD4-2ZWP-QJAA-8ADE`。
- 共 7 次 HTTP 请求，包含 6 个新动作和 1 次相同请求 ID、相同内容的重放。全部 HTTP 200 且 `accepted=true`。
- `/enter` 成功，返回现实剩余时间 1200 秒。
- 检测 `(300,400)`、频道 1 后为 105 虚拟秒；同点换至频道 2 检测后为 111 秒。
- 重放上一检测后仍为 111 秒，并返回相同服务器时间戳，验证本次幂等重放未重复计费。
- `/clear` 在 `(300,0)` 对频道 3 正常返回 `no_target_in_range`，累计 194 秒。
- 在 `(300,0)` 对频道 2 再检测后为 199 秒，符合清除操作不切换测向机频道的规则。
- `/exit` 返回 `user_exit`，累计仍为 199 秒；GUI 显示正常结束。
- 客户端整段请求处理约 0.184 秒。该数值不含 GUI 开局准备、登录或日志导出，也不能外推为完整算法耗时。
- GUI 显示本案例共 10 个全向源、0 个定向源，行为日志已保存，文件名 `practice-p3-3138906483235560740-RCD4-2ZWP-QJAA-8ADE.jlog`，显示大小 8.6 KiB。

实际测量均返回 `no_signal`，清除返回范围内无目标，因此本轮没有成功定位或清除目标。它证明本次问题 3 演练的启动、通信、计时、正常退出和自动保存链路可用，不证明算法清除率、定向源处理或完整求解能力。没有启动任何正式测试，也没有消费正式测试机会；未进入正式页面核对剩余次数。

## 中断及范围关闭

在尝试返回演练列表时，Computer Use 报告用户按下物理 Escape 键停止，代理立即停止全部后续桌面操作。该点击是否完成不能从中断回执确定。问题 4 演练、日志导出及上传均未验证。用户随后明确“我最终证实打A 不考虑B了”，本分支关闭，不继续上述事项。

## 证据

- [脱敏 HTTP 请求响应与八项检查](evidence/q3-http-smoke.json)：八项检查均通过。
- [Computer Use 实际 GUI 结果文本](evidence/q3-gui-result.txt)：包含完成弹窗、七条动作反馈和日志保存提示。
- [接口附件核对](B题_接口核对.md)：与原始 DOCX 的对应说明。

原始 `.jlog` 由模拟器保存在软件自己的目录中；本轮没有导出，也未声称该文件已在本记录目录或 GitHub 备份。保存的 GUI 文本与 HTTP 记录可供复核本次接口检查，不作为 A 题正式模型结果或工作流门禁通过证据。
